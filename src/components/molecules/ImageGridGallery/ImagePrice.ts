import { Box } from '@material-ui/core'
import styled from 'styled-components'

const ImagePrice = styled(Box)`
	text-align: center;
	font-weight: 300 !important;
	font-size: 18px;
	font-family: 'Roboto';
	margin-bottom: 15px;
`

export default ImagePrice
