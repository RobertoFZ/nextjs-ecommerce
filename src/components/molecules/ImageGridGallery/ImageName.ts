import { Box } from '@material-ui/core'
import styled from 'styled-components'

const ImageImage = styled(Box)`
	text-align: center;
	font-weight: 500 !important;
	font-size: 22px;
	font-family: 'Roboto';
	margin-bottom: 15px;
`

export default ImageImage
